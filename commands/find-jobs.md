---
description: Search for relevant job listings and return a ranked list with suitability scores.
argument-hint: "[role or location override — optional]"
---

The user has run /find-jobs. Execute the `job-finder` skill: search for relevant job listings across the user's target job boards using their stored profile, and return a ranked list with suitability scores.
