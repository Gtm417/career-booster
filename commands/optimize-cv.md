---
description: Improve your existing CV for ATS compatibility and recruiter attractiveness.
argument-hint: "[language code, e.g. en | pl — optional, defaults to base CV language]"
---

The user has run /optimize-cv. Execute the `cv-optimizer` skill: improve the user's base CV for ATS compatibility and recruiter attractiveness without changing factual content.
